#include <stdio.h>
#include <conio.h>
int main()
{
    int n;
    system("cls");
    printf("Enter n Value ");
    scanf("%d", &n);
    if (n > 0)
    {
        printf("Yes n is +ve");
    }
    else
    {
        printf("Yes n is -ve");
    }
    return 0;
}