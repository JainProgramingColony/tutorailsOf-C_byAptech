#include <stdio.h>
#include <conio.h>
int main()
{
    system("cls");
    int a, b, sum;
    printf("Enter a,b Value ");
    scanf("%d%d", &a, &b);
    sum = a + b;
    printf("Sum= %d", sum);
    return 0;
}